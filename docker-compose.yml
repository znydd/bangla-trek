services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bangla_trek
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
