[project]
name = "bangla-trek"
version = "0.1.0"
description = "FastAPI + SQLModel + PostgreSQL app for BongoVromon"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "sqlmodel>=0.0.22",
    "uvicorn[standard]>=0.30.0",
    "psycopg[binary]>=3.2.1",
    "alembic>=1.13.2",
    "python-dotenv>=1.2.2",
]
